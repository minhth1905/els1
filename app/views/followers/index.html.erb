<%= provide :title, t("relationship.follower") %>
<h4>
  <%= t "relationship.number_follower", number: @users.count %>
<h4>
<%= will_paginate @users %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate @users %>
